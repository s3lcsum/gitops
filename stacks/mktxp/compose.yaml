# github.com/s3lcsum/gitops/stacks/mktxp/compose.yaml
# WARNING: This file should not be edited anywhere except the git repository
# Any local changes will be overwritten by the git repository
#
# MKTXP Stack
# Mikrotik Prometheus exporter
# Maintainer: Dominik Siejak

services:
  mktxp:
    image: ghcr.io/akpw/mktxp:latest
    container_name: mktxp
    restart: unless-stopped
    networks:
      - metrics
    volumes:
      - /Portainer/mktxp/mktxp.conf:/root/mktxp/mktxp.conf


networks:
  metrics:
    external: true
