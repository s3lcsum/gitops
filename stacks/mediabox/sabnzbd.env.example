# All SABnzbd configuration is managed through the web UI at first run
# and persisted in the sabnzbd_config volume (/config/sabnzbd.ini).
# The linuxserver/sabnzbd image does not read any environment variables
# for application configuration. This file exists only for reference.
