# github.com/s3lcsum/gitops/stacks/homarr/compose.yaml
# WARNING: This file should not be edited anywhere except the git repository
# Any local changes will be overwritten by the git repository
#
# Homarr Stack
# Self-hosted dashboard for your services
# Maintainer: Dominik Siejak

services:
  homarr:
    image: ghcr.io/ajnart/homarr:latest
    container_name: homarr
    restart: unless-stopped
    ports:
      - 7575:7575
    networks:
      - proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - homarr-data:/app/data/configs
    labels:
      traefik.enable: "true"
      traefik.http.routers.homarr.rule: Host(`home.wally.dominiksiejak.pl`)
      traefik.http.routers.homarr.entrypoints: websecure

volumes:
  homarr-data:

networks:
  proxy:
    external: true
