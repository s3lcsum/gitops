# https://github.com/s3lcsum/gitops/blob/main/stacks/cloudflared/compose.yaml

services:
  cloudflared:
    image: cloudflare/cloudflared:2026.7.3
    container_name: cloudflared
    restart: unless-stopped
    env_file:
      - /opt/cloudflared/cloudflared.env
    command: tunnel run
