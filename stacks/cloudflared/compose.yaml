
# github.com/s3lcsum/gitops/stacks/cloudflared/compose.yaml

services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel run
    env_file:
      - /opt/cloudflared/cloudflared.env
