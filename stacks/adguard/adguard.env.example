# AdGuard Home Configuration
# Basic configuration is handled through the web UI at http://adguard.lake.dominiksiejak.pl
# Access the web interface after first startup to complete setup

# Timezone (optional - defaults to UTC)
TZ=Europe/Warsaw

# Admin username (set through web UI on first startup)
# ADGUARD_USERNAME=admin

# Admin password (set through web UI on first startup)
# ADGUARD_PASSWORD=your-secure-password

# Web interface port (internal container port)
# ADGUARD_WEB_PORT=80

# DNS server port (internal container port)
# ADGUARD_DNS_PORT=53
