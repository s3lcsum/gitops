
# github.com/s3lcsum/gitops/stacks/uptime_kuma/compose.yaml

services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    networks:
      - metrics
      - proxy
    ports:
      - 3001:3001
    volumes:
      - uptime-kuma-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      traefik.enable: "true"
      traefik.http.routers.uptime-kuma.rule: Host(`uptime.lake.dominiksiejak.pl`)

volumes:
  uptime-kuma-data:

networks:
  metrics:
    external: true
  proxy:
    external: true
