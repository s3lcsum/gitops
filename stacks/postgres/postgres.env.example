# PostgreSQL Admin Credentials
PGDATA=/var/lib/postgresql/data/pgdata

POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-postgres-admin-password

# Postgres Exporter integration
DATA_SOURCE_NAME=postgresql://postgres:your-secure-postgres-admin-password@postgres:5432/postgres?sslmode=disable

# Note: Application database users are managed by Vault
# Use: vault read database/static-creds/<app>-static
# Or:  vault read database/creds/<app>
