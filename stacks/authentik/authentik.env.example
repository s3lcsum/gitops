# Authentik Configuration
# https://docs.goauthentik.io/docs/installation/configuration

# Secret key used for cookie signing and database encryption
# Generate with: openssl rand -base64 50
AUTHENTIK_SECRET_KEY=your-secret-key-at-least-50-characters-long-here

# PostgreSQL Password
# Must match DBINIT_AUTHENTIK_PASSWORD in stacks/postgres/postgres.env
AUTHENTIK_POSTGRESQL__PASSWORD=your-secure-database-password-here

# Bootstrap Configuration (used on first startup only)
# Password for the default akadmin user
AUTHENTIK_BOOTSTRAP_PASSWORD=your-admin-password-here

# API Token for Terraform provider authentication
# This token will be created automatically on first boot
AUTHENTIK_BOOTSTRAP_TOKEN=your-bootstrap-token-for-terraform-here

# Email for the admin user
AUTHENTIK_BOOTSTRAP_EMAIL=dreewniak@gmail.com

# Optional: Email Configuration
# AUTHENTIK_EMAIL__HOST=smtp.example.com
# AUTHENTIK_EMAIL__PORT=587
# AUTHENTIK_EMAIL__USERNAME=authentik@example.com
# AUTHENTIK_EMAIL__PASSWORD=your-smtp-password
# AUTHENTIK_EMAIL__USE_TLS=true
# AUTHENTIK_EMAIL__FROM=authentik@example.com
