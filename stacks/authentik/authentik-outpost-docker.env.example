# Authentik Docker Outpost Configuration
# https://docs.goauthentik.io/docs/outposts/docker

# Authentik API Token for the outpost
# Generate a token in Authentik for the outpost service account
AUTHENTIK_TOKEN=your-outpost-api-token-here

