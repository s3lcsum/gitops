# DNS Configuration for RouterOS
#
# Note: DNS records are managed by AdGuard DNS server (192.168.89.251)
# RouterOS only acts as a DNS client, not a DNS server
# All DNS record management should be done through AdGuard configuration
