apiVersion: v2
name: velero
version: 0.1.0
description: Velero — Kubernetes cluster backup and restore
dependencies:
  - name: velero
    version: "8.2.0"
    repository: "https://vmware-tanzu.github.io/helm-charts"
