apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - argocd.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
