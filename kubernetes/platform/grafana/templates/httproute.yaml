apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - grafana.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: grafana
          port: 80
