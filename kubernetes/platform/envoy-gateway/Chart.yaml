apiVersion: v2
name: envoy-gateway
version: 0.1.0
description: Envoy Gateway — Kubernetes Gateway API implementation
dependencies:
  - name: gateway-helm
    version: "1.7.1"
    repository: "oci://docker.io/envoyproxy"
