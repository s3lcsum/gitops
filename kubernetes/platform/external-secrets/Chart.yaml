apiVersion: v2
name: external-secrets
version: 0.1.0
description: External Secrets Operator — sync secrets from HashiCorp Vault
dependencies:
  - name: external-secrets
    version: "0.14.3"
    repository: "https://charts.external-secrets.io"
