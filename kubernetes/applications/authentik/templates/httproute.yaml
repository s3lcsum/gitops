apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: authentik
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - auth.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: authentik-server
          port: 80
