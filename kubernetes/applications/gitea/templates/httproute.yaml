apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gitea
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - gitea.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: gitea-http
          port: 3000
