apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: netbox
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - netbox.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: netbox
          port: 80
