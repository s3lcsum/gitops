apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
  hostnames:
    - vault.lake.dominiksiejak.pl
  rules:
    - backendRefs:
        - name: vault-ui
          port: 8200
