# Creates argo-workflows-sso for Authentik OIDC (client-id + client-secret).
# 1Password item: Servers / argo-workflows-oidc / credential
# Tag: ArgoCD External Secrets Operator
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argo-workflows-oidc
  namespace: argo-workflows
  labels:
    app.kubernetes.io/name: argo-workflows-oidc
    app.kubernetes.io/part-of: argo-workflows
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: argo-workflows-sso
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        client-id: workflows
        client-secret: "{{ .credential }}"
  data:
    - secretKey: credential
      remoteRef:
        key: argo-workflows-oidc/credential
