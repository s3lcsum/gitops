apiVersion: v2
name: cilium
description: Cilium CNI and Gateway API for the kubeadm node. Upstream chart is vendored under charts/cilium.
type: application
version: 0.1.0
appVersion: v1.20.2
dependencies:
  - name: cilium
    version: 1.20.2
    repository: file://charts/cilium
